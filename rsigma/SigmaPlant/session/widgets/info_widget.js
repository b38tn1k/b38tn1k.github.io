class InfoWidget extends Widget {
    // informational widget for displaying data on process blocks
    // e.g the contained categories and any metrics
    constructor(parent, key='infor_widget', fill='full') {
        super(parent, key, fill);
        
    }
}
